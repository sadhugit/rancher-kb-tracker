name: Generate RSS

on:
  issues:
    types: [opened, edited, closed, labeled]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Install Dependencies
      run: pip install feedgen requests

    - name: Generate RSS
      run: python scripts/generate_rss.py

    - name: Commit RSS
      run: |
        git config user.name "Suresh Sadhu"
        git config user.email "ranchersadhu@gmail.com"
        git add rss.xml
        git commit -m "Update RSS"
        git push
